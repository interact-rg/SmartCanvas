version: "3.8"

services:
  web:
    build: .
    env_file:
      - .env  # Load environment variables from the .env file
    ports:
      - "5000:5000"
    restart: on-failure
    volumes:
      - .:/smart-canvas  # Syncs local changes instantly